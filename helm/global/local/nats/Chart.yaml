apiVersion: v2
name: nats-local
version: 0.1.0
description: NATS server for the local k3d videocall stack — single replica, auth on from day one

dependencies:
  - name: nats
    version: 0.19.15
    repository: https://nats-io.github.io/k8s/helm/charts/
